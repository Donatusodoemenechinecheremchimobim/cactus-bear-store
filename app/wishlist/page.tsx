"use client"; import { useDB } from '@/store/useDB'; import Link from 'next/link'; import { Trash2 } from 'lucide-react'; export default function Wishlist() { const { wishlist, products, toggleWishlist } = useDB(); const savedItems = products.filter(p => wishlist.includes(p.id)); return (<div className="min-h-screen pt-32 px-6 bg-black text-white"><div className="max-w-6xl mx-auto"><h1 className="text-6xl font-black uppercase italic text-brand-neon mb-12">Saved Stash</h1>{savedItems.length === 0 ? <div className="text-white/30 font-mono uppercase text-xl">Your stash is empty.</div> : <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">{savedItems.map(p => <div key={p.id} className="group relative bg-[#0a0a0a] border border-white/10"><div className="h-64 bg-cover bg-center grayscale group-hover:grayscale-0 transition-all" style={{ backgroundImage: `url(${p.images[0]})` }} /><div className="p-6"><h3 className="text-2xl font-black uppercase italic mb-2">{p.name}</h3><div className="flex justify-between items-center"><span className="text-brand-neon font-mono">$${p.price}</span><div className="flex gap-4"><button onClick={() => toggleWishlist(p.id)} className="text-white/40 hover:text-red-500"><Trash2 className="w-5 h-5" /></button><Link href={`/product/${p.id}`} className="bg-white text-black px-4 py-1 text-xs font-bold uppercase hover:bg-brand-neon">View</Link></div></div></div></div>)}</div>}</div></div>); }